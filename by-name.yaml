detectors:
- name: detect-by-name
  keywords:
  - key
  - api
  - secret
  - client
  - passwd
  - password
  - auth
  - access
  regex:
    generic-api-key: "(?i)(?:key|api|token|secret|client|passwd|password|auth|access)\\s*=\\s*['\"]([0-9a-zA-Z\\-_=]{8,64})['\"]"
    